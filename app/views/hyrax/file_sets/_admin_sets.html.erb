<% if Flipflop.assign_admin_set? %>
  <%= select_tag :admin_set_id,
               options_for_select(admin_set_options.map { |option| [option[0], option[1], data: option[2]] }),
               include_blank: false,
               class: 'form-control' %>
<% end %>
